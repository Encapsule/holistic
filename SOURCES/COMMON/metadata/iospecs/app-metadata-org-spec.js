"use strict";

// app-metadata-org-spec.js
//
// Defines the schema of a developer-specified descriptor object containing
// metadata about the organization that owns/publishes this application.
module.exports = {
  ____accept: "jsObject" // extend the the base org metadata format by adding filter spec namespaces below.

};